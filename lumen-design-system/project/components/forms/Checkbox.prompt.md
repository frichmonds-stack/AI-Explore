Checkbox for filter sidebars — pine fill when checked, optional trailing result count.

```jsx
<Checkbox label="Approved" count={28} defaultChecked />
<Checkbox label="Lesson planning" count={14} />
```

Props: `label · count · round` plus all native input props.
